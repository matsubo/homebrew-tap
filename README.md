# matsubo/homebrew-tap

Homebrew tap for matsubo's tools.

## Available formulae

- `vmt` — Voice Memos transcription CLI ([source](https://github.com/matsubo/voice-memo-stt))

## Usage

```bash
brew install matsubo/tap/vmt
```
